<p id="notice"><%= notice %></p>

<dl class="dl-horizontal">
  <p>
    <strong>Name:</strong>
    <%= @image.name %>
  </p>

  <p>
    <strong>Picture:</strong>
  </p>
    <%= image_tag(@image.picture.url, width: "30%") if @image.picture? %>
</dl>


<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
